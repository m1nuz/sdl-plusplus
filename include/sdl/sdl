// -*- C++ -*-

#pragma once

#include <sdl/init>
#include <sdl/window>
#include <sdl/renderer>
#include <sdl/event>
#include <sdl/graphic_context>

namespace sdl {
    using window = detail::window;
    using renderer = detail::renderer;
    using event = detail::event;
    using graphic_context = detail::graphic_context;

    using point = detail::point;
} // namespace sdl
